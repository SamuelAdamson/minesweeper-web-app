import { Cell } from '..'

type Props = {
  mode: string;
}

const gridSize: object = {
  easy : [8, 12],
  medium : [12, 16],
  hard : [16, 20]
}

export const Grid = ( { mode } : Props ) => {

	return (
    <>
    
    </>
  )

}